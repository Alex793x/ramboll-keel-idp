# {{ project_name }}

> {{ description }}

Owned by the **{{ department.name }}** department
({% for u in users %}{{ u.name }}{% if not loop.last %}, {% endif %}{% endfor %}).
Generated from the Ramboll Developer Platform (Keel) `python-service`
golden-path blueprint.

## What this is

`{{ project_name }}` is a Ramboll {{ service_kind }} service built on the Python
golden path: an opinionated, standards-compliant layout that is **green from
birth** — it compiles and passes its smoke + property tests on the first commit.

## Where to go next

- **[Getting started](getting-started.md)** — install, test, and run locally.
- **[Architecture](architecture.md)** — context, components, and data flow.
- **[Runbook](runbook.md)** — operate, monitor, and recover the service.
- **[Contributing](contributing.md)** — branching, PRs, and the quality gates.
- **[ADR-0001](adr/0001-record-architecture-decisions.md)** — how we record decisions.

## Standards, as code

The non-negotiable engineering standards are embedded as AI agent skills under
`.claude/skills/` so any coding agent adopts them automatically:

- **property-based-testing** — every pure function ships with Hypothesis properties.
- **python-clean-code** — small, typed, lint/format/type-clean functions.
- **git-ci-governance** — `main`/`dev`/`staging`, strict branch naming, reusable CI.

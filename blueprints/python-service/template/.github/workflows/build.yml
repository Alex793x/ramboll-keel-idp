# Build — references the central reusable workflow so updates propagate
# everywhere. Do NOT inline pipeline logic here (see .claude/skills/git-ci-governance).
name: build

on:
  push:
    branches: [main, dev, staging]
  pull_request:

jobs:
  build:
    uses: Ramboll-RDP/keel/.github/workflows/reusable-build.yml@v1
    with:
      python-version: "3.12"

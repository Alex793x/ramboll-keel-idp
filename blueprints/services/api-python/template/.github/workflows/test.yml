# test — dispatched to the shared, language-aware pipeline. EVERY Keel repo points at the SAME
# reusable workflow; the 'stack' input selects the right test composite (SPEC §14). Do NOT
# inline pipeline logic here. The check name 'test' is part of the branch-protection contract.
name: test

on:
  push:
    branches: [main, dev, staging]
  pull_request:

jobs:
  test:
    uses: Alex793x/keel/.github/workflows/reusable-test.yml@main
    with:
      stack: python

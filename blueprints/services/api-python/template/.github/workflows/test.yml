# Test — runs pytest (incl. Hypothesis property tests) via the central reusable
# workflow. Do NOT inline pipeline logic here (see .claude/skills/git-ci-governance).
name: test

on:
  push:
    branches: [main, dev, staging]
  pull_request:

jobs:
  test:
    uses: Alex793x/keel/.github/workflows/reusable-test.yml@main
    with:
      python-version: "3.12"

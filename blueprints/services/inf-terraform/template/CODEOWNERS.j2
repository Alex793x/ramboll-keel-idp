# CODEOWNERS — required reviewers for {{ project_name }}.
# Owners: the {{ department.name }} department, represented by the selected users below.
# Branch protection on `main` requires CODEOWNERS approval (see CONTRIBUTING.md).
# On a GitHub org you may also grant the whole team: @<your-org>/{{ department.team_slug }}
# Syntax: https://docs.github.com/articles/about-code-owners

* {% for u in users %}@{{ u.github_login }}{% if not loop.last %} {% endif %}{% endfor %}

# Keep CI governance and the embedded standards owned explicitly.
/.github/ {% for u in users %}@{{ u.github_login }}{% if not loop.last %} {% endif %}{% endfor %}
/.claude/ {% for u in users %}@{{ u.github_login }}{% if not loop.last %} {% endif %}{% endfor %}

# Validate — ruff, black --check, mypy, branch-name check and mkdocs --strict via
# the central reusable workflow. Do NOT inline pipeline logic here
# (see .claude/skills/git-ci-governance).
name: validate

on:
  push:
    branches: [main, dev, staging]
  pull_request:

jobs:
  validate:
    uses: Alex793x/keel/.github/workflows/reusable-validate.yml@main
    with:
      python-version: "3.12"

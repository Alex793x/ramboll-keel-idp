// {{ project_name }} — {{ description }}
//
// Minimal ASP.NET Core (net8.0) API. The status payload logic is pure and
// lives in StatusInfo.cs so it stays unit-testable without spinning up the
// host; this file is only the wiring.
//
// Generated by Keel (Ramboll Developer Platform) from the `api-dotnet`
// service blueprint. Owned by the {{ department.name }} department.

using Api;

var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.MapGet("/health", () => StatusInfo.Create("{{ project_name }}"));

app.Run();

# ── Rust ───────────────────────────────────────────────
/target/
**/*.rs.bk
# Cargo.lock IS committed (this is an application workspace, not a library)

# ── Node / TanStack Start hub ──────────────────────────
node_modules/
hub/node_modules/
hub/dist/
hub/.output/
hub/.nitro/
hub/.tanstack/
hub/.vinxi/
*.tsbuildinfo
coverage/

# ── Keel runtime / generated output ────────────────────
/generated/
keel.db
*.sqlite3

# ── MemTrace local store ───────────────────────────────
/.memdb/

# ── Python (legacy v1 + generated-repo local envs) ─────
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
*.egg-info/

# ── Editors / OS ───────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# ── Secrets ────────────────────────────────────────────
.env
.env.*
!.env.example
